#include "main.h"
/**
 * add - adds two integers and returns the result
 * @i: number one
 * @j: number two
 * Return: Addition of two integers number one and number two
 */

int add(int i, int j)
{
	return (i + j);
}
